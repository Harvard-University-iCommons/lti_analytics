To install the git hooks in this directory for this repository only, create symbolic links in the lti_emailer/.git/hooks directory:

Example:

cd ~/tlt/lti_analytics
ln -s ~/tlt/lti_analytics/.git_template/hooks/pre-commit ~/tlt/lti_analytics/.git/hooks/pre-commit
